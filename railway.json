{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "npm start & cd /app/backend && python -m uvicorn main:app --host 0.0.0.0 --port 8000"
  }
}
