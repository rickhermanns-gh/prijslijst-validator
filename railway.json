{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "echo RAILWAY_START; cd /app/backend; python -m uvicorn main:app --host 0.0.0.0 --port 8000 & cd /app; npm start"
  }
}
